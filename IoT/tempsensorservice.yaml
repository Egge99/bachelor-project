apiVersion: v1
kind: Service
metadata:
  name: temp-sensor-service
  namespace: iot
spec:
  selector:
    app: temp-sensor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
